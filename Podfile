platform :ios, '8.0'
use_frameworks!

def import_pods

#pod 'ReactiveKit', '~> 1.1.3'
#pod 'ReactiveUIKit', '~> 1.0.10'
#pod 'ReactiveFoundation'#, '~> 1.1.1'
pod 'iAsync.utils', :path => '.'

end

target 'iAsync.utils' do
  import_pods
end

target 'iAsync.utilsTests' do
  import_pods
end

##link_with 'iAsync.utils', 'iAsync.utilsTests'
